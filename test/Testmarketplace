pragma solidity ^0.4.23;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/Marketplace.sol";

contract TestMarketplace {
	Marketplace storeowner = Marketplace(DeployedAddresses.Marketplace());


    //Testing create store function
function testGetStoreownerAddressByStoreId() public {
 
  address expected = this;

  address storeowner = AddStore.owner;


  Assert.equal(storeowner, expected, "Adoption of pet ID 8 should be recorded.");
}
}